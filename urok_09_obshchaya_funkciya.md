# Урок 09. Универсальный Грам–Шмидт: цикл вместо копипасты

Цель урока: написать ОДНУ функцию, которая делает Грам–Шмидт для любого
количества векторов. Это ровно та функция `gsBasis`, которую просят написать
в заданиях курсов по линейной алгебре.

---

## 1. Ищем узор в коде урока 08

Посмотри, что мы делали с каждым столбцом:

```
столбец 0:  (ничего не вычитаем)                нормализуем
столбец 1:  вычесть тень на столбец 0           нормализуем
столбец 2:  вычесть тени на столбцы 0, 1        нормализуем
столбец 3:  вычесть тени на столбцы 0, 1, 2     нормализуем
```

Узор: **для столбца номер j вычитаем тени на ВСЕ столбцы с номерами
МЕНЬШЕ j, потом нормализуем.**

А в уроке 05 (раздел 7) мы учили конструкцию ровно под этот узор:

```python
for j in range(...):        # для каждого столбца j
    for i in range(j):      # для каждого предыдущего столбца i (i < j)
        ...                 # вычесть тень столбца j на столбец i
```

Помнишь, `range(j)` — это числа `0, 1, ..., j-1`, а при `j = 0` внутренний цикл
не выполняется ни разу? Идеально: у первого вектора предшественников нет,
его мы только нормализуем.

---

## 2. Универсальная функция

```python
import numpy as np
import numpy.linalg as la

verySmallNumber = 1e-14


def gsBasis(A):
    """Процесс Грама-Шмидта для ЛЮБОГО числа векторов-столбцов."""

    B = np.array(A, dtype=np.float64)        # копия в дробных числах

    for j in range(B.shape[1]):              # проходим по всем столбцам
        for i in range(j):                   # по всем ПРЕДЫДУЩИМ столбцам
            B[:, j] = B[:, j] - B[:, j] @ B[:, i] * B[:, i]   # минус тень

        if la.norm(B[:, j]) > verySmallNumber:
            B[:, j] = B[:, j] / la.norm(B[:, j])     # нормализуем
        else:
            B[:, j] = np.zeros_like(B[:, j])         # зависимый → ноль

    return B
```

Одиннадцать строк — и работает для 2, 3, 10, 100 векторов.

---

## 3. Разбор новых строк

### `B.shape[1]` — сколько столбцов в матрице

```python
B.shape       # пара чисел: (строк, столбцов). Например (3, 4)
B.shape[0]    # число строк
B.shape[1]    # число столбцов ← нам нужно это: столбцы = векторы
```

`shape` по-английски «форма». `range(B.shape[1])` — «пробеги номера всех
столбцов»: если их 4, то j будет 0, 1, 2, 3.

### Двойной цикл — проследим по шагам

Пусть в матрице 3 столбца. Вот ВСЯ история выполнения:

```
j = 0:  внутренний цикл пуст (range(0))     → нормализуем столбец 0
j = 1:  i = 0: вычли тень на столбец 0      → нормализуем столбец 1
j = 2:  i = 0: вычли тень на столбец 0
        i = 1: вычли тень на столбец 1      → нормализуем столбец 2
```

Сравни с планом из раздела 1 — совпадает один в один. Это в точности
код урока 08, только «раскопированные» блоки заменены циклом.

### Почему тень считается на столбец i, а он уже единичный?

Ключевой момент! Когда мы обрабатываем столбец j, все столбцы с номерами
меньше j **уже обработаны на предыдущих оборотах цикла** — они уже
нормализованы (или обнулены). Поэтому формула тени `(v·e)e`, которая требует
единичный `e` (урок 04, раздел 2), применяется законно.

А если предыдущий столбец обнулён? Тогда `B[:, j] @ B[:, i]` — умножение
на нули — даст 0, тень будет нулевой, и вычитание ничего не изменит.
Обнулённые столбцы автоматически «не мешают». Красиво.

---

## 4. Тестируем со всех сторон

```python
# Тест 1: пример из уроков 07-08 — должен совпасть
V = np.array([[1, 2, 3],
              [1, 0, 1],
              [1, 1, -1]], dtype=np.float64)
E = gsBasis(V)
print(E)
print(E.T @ E)            # почти единичная матрица = всё перпендикулярно и единично

# Тест 2: 4 вектора в 4D — раньше пришлось бы писать новую функцию!
V4 = np.array([[1, 0, 2, 6],
               [0, 1, 8, 2],
               [2, 8, 3, 1],
               [1, -6, 2, 3]], dtype=np.float64)
print(gsBasis(V4))

# Тест 3: зависимые векторы — ждём нулевой столбец
V_zav = np.array([[1, 2, 1],
                  [0, 0, 1],
                  [0, 0, 0]], dtype=np.float64)   # v2 = 2×v1
print(gsBasis(V_zav))

# Тест 4: применить gsBasis к УЖЕ ортонормированному результату —
# ничего не должно измениться (тени нулевые, длины уже 1)
print(gsBasis(E))
```

Тест 4 — интересный способ самопроверки: Грам–Шмидт от «ровного» набора
ничего не меняет. Если меняет — в функции ошибка.

### Бонус: считаем независимые направления (ранг)

```python
def dimensions(A):
    """Сколько независимых направлений в наборе векторов."""
    return np.sum(la.norm(gsBasis(A), axis=0))
```

Как это работает:

- `gsBasis(A)` — сделали Грам–Шмидт: каждый столбец теперь длиной 1 или 0;
- `la.norm(..., axis=0)` — посчитать длину КАЖДОГО столбца
  (`axis=0` значит «вдоль строк», то есть по столбцам); получится
  список из единиц и нулей;
- `np.sum(...)` — сложить. Сумма единиц = число настоящих направлений.

Проверь: `dimensions(V)` даст `3.0`, а `dimensions(V_zav)` даст `2.0`.

---

## 5. Частые ошибки (проверь себя, если функция не работает)

| Симптом | Причина |
|---|---|
| в ответе нули там, где не ждали | забыл `dtype=np.float64` — целые округлились |
| `E.T @ E` не единичная | вычитаешь тень на столбец `j` вместо `i`, или перепутал `@` и `*` |
| ошибка деления на ноль | нет проверки `if la.norm(...) > verySmallNumber` |
| результат меняет исходную матрицу | забыл сделать копию `B = np.array(A, ...)` |
| работает для 3 векторов, падает для 4 | где-то осталось «жёсткое» число 3 вместо `B.shape[1]` |

---

## 6. Мини-задачи

1. Не глядя в урок, напиши `gsBasis` заново по памяти. Проверь тестом 1.
   (Это лучшее упражнение урока. Не пропускай!)
2. Прогони через `gsBasis` матрицу из ДВУХ столбцов в 3D:
   `v1 = [3, 0, 0]`, `v2 = [1, 1, 0]`. Предскажи ответ на бумаге до запуска.
3. Что вернёт `dimensions` для матрицы, где все три столбца равны `[1, 1, 1]`?

---

## Ответы

2. Матрица и предсказание:

```python
V = np.array([[3, 1],
              [0, 1],
              [0, 0]], dtype=np.float64)
print(gsBasis(V))
# e1 = [1, 0, 0]   (v1 вдоль оси x, нормализовали)
# e2 = [0, 1, 0]   (у v2 убрали горизонтальную тень [1,0,0], остался [0,1,0])
```

3. `1.0` — направление одно, второй и третий столбцы обнулятся.

---

Поздравляю: ты умеешь и вручную, и в коде, и для любого размера.
Дальше — самое красивое применение: `urok_10_smena_bazisa_i_otrazhenie.md`,
где Грам–Шмидт помогает отражать объекты в «кривом» зеркале.
