# Урок 06. NumPy: матрицы, столбцы и весь инструментарий Грама–Шмидта

Цель урока: после него ты сможешь прочитать ЛЮБУЮ строку из кода
Грама–Шмидта. Здесь собраны все операции NumPy, которые встретятся дальше.

---

## 1. Вектор в NumPy (повторение)

```python
import numpy as np
import numpy.linalg as la

v = np.array([1, 2, 3])   # вектор из трёх чисел
```

`np.array` превращает список в объект, который умеет математику:
складываться по позициям, умножаться на число и так далее (урок 02).

---

## 2. Матрица: таблица чисел

Матрица — это просто прямоугольная таблица чисел.

```python
A = np.array([[1, 2, 3],
              [4, 5, 6]])
```

Смотри на скобки:

- внешние `[...]` — вся матрица;
- каждые внутренние `[...]` — одна **строка**.

Получилась таблица 2 строки × 3 столбца:

```
1 2 3
4 5 6
```

Записывать можно и в одну строку кода — Python всё равно:
`np.array([[1, 2, 3], [4, 5, 6]])`. В несколько строк — просто читабельнее.

---

## 3. Как достать строку и столбец

```python
A = np.array([[1, 2, 3],
              [4, 5, 6]])

print(A[0])       # [1 2 3]  — строка номер 0 (первая)
print(A[1])       # [4 5 6]  — строка номер 1 (вторая)

print(A[:, 0])    # [1 4]    — столбец номер 0 (первый)
print(A[:, 2])    # [3 6]    — столбец номер 2 (третий)

print(A[1, 2])    # 6        — строка 1, столбец 2: одно число
```

Разбор записи `A[:, 0]` — она будет ВЕЗДЕ, разберём по символам:

- В квадратных скобках два места через запятую: `A[какие_строки, какие_столбцы]`.
- Двоеточие `:` означает «ВСЕ». 
- Значит `A[:, 0]` = «все строки, столбец 0» = **весь первый столбец**.
- А `A[0]` — сокращение от `A[0, :]` = «строка 0, все столбцы» = первая строка.

Мнемоника: **запятая разделяет "строки, столбцы"; двоеточие = «всё»**.

---

## 4. Почему это важно: векторы задачи лежат в СТОЛБЦАХ

В задачах Грама–Шмидта несколько векторов упаковывают в одну матрицу
**как столбцы**. Это соглашение курса линейной алгебры, привыкни к нему:

```python
A = np.array([[1, 2, 3],
              [1, 0, 1],
              [1, 1, -1]], dtype=np.float64)
```

Таблица:

```
 1   2   3
 1   0   1
 1   1  -1
 ^   ^   ^
 v1  v2  v3     ← читаем СВЕРХУ ВНИЗ по столбцам!
```

Значит:

```python
print(A[:, 0])   # [1. 1. 1.]   — вектор v1
print(A[:, 1])   # [2. 0. 1.]   — вектор v2
print(A[:, 2])   # [3. 1. -1.]  — вектор v3
```

Частая ошибка новичка: прочитать векторы по строкам. НЕТ: по столбцам.

Столбцу можно и ПРИСВОИТЬ новое значение — так Грам–Шмидт будет
перезаписывать векторы по одному:

```python
A[:, 0] = A[:, 0] / la.norm(A[:, 0])   # заменить первый столбец
                                       # его нормализованной версией
```

---

## 5. dtype=np.float64 — почему без него всё ломается

Смотри на ловушку:

```python
X = np.array([[1, 2],
              [3, 4]])          # матрица из ЦЕЛЫХ чисел
X[:, 0] = X[:, 0] / la.norm(X[:, 0])
print(X[:, 0])                  # [0 0] ← ВСЁ ОКРУГЛИЛОСЬ ДО НУЛЯ!
```

Матрица создана из целых чисел, поэтому NumPy решил: «храню только целые».
Записываешь в неё дроби 0.316..., 0.948... — они молча обрубаются до 0.

Лекарство:

```python
X = np.array([[1, 2],
              [3, 4]], dtype=np.float64)   # храни как дробные!
```

`float64` = «дробное число двойной точности», стандартный тип для вычислений.
Поэтому в начале функции Грама–Шмидта всегда есть строка:

```python
B = np.array(A, dtype=np.float64)
```

Она делает сразу ДВА дела:

1. **копию** матрицы A (мы будем менять столбцы — портить исходник невежливо);
2. перевод в **дробные числа** (см. ловушку выше).

Примечание: в старых учебных ноутбуках пишут `np.float_` — в новых версиях
NumPy это имя удалено и даст ошибку. Пиши `np.float64`.

---

## 6. Четыре умножения — не перепутай

| Запись | Что делает | Результат |
|---|---|---|
| `u @ v` | скалярное произведение векторов | число |
| `u * v` | поэлементное умножение векторов | вектор |
| `2 * v` | умножение вектора на число | вектор |
| `A @ v` | матрица действует на вектор | вектор |
| `A @ B` | умножение матрицы на матрицу | матрица |

`A @ v` и `A @ B` понадобятся в уроке 10 (отражения). Пример:

```python
A = np.array([[1, 0],
              [0, -1]])
v = np.array([2, 3])
print(A @ v)      # [ 2 -3]
```

Матрица `A` — «машинка, преобразующая векторы». Эта конкретная машинка
оставила x и перевернула знак y — это отражение относительно оси x!
Запомни её, она вернётся в уроке 10.

---

## 7. Остальные инструменты (по одному предложению)

```python
la.norm(v)          # длина вектора (урок 03)
np.sqrt(x)          # квадратный корень
A.T                 # транспонирование: строки и столбцы меняются местами
la.inv(A)           # обратная матрица ("отменить действие A"); понадобится в уроке 10
np.zeros_like(v)    # вектор той же формы, что v, но из одних нулей
np.eye(3)           # единичная матрица 3×3 (единицы на диагонали) — "ничего не делать"
v.copy()            # копия вектора
```

Про `A.T` чуть подробнее, он важен:

```python
A = np.array([[1, 2, 3],
              [4, 5, 6]])
print(A.T)
# [[1 4]
#  [2 5]
#  [3 6]]
```

Каждая строка стала столбцом. Зачем это нужно — узнаешь в уроке 10:
для «хороших» (ортонормированных) матриц транспонирование заменяет
обратную матрицу, и это дико удобно.

---

## 8. Проверка себя: прочитай код ДО запуска

Предскажи каждый print:

```python
import numpy as np
import numpy.linalg as la

A = np.array([[3, 1],
              [4, 1]], dtype=np.float64)

print(A[:, 0])                 # ?
print(la.norm(A[:, 0]))        # ?

A[:, 0] = A[:, 0] / la.norm(A[:, 0])
print(A[:, 0])                 # ?
print(A[:, 1])                 # ?
print(A[:, 1] @ A[:, 0])       # ?
```

<details>
<summary>Ответы (нажми, когда решишь)</summary>

```
[3. 4.]        — первый столбец
5.0            — √(9+16)
[0.6 0.8]      — нормализованный первый столбец
[1. 1.]        — второй столбец не трогали
1.4            — 1×0.6 + 1×0.8
```
</details>

Если ты правильно предсказал последнюю строку — поздравляю: ты уже
выполняешь первые шаги Грама–Шмидта в голове.

---

## 9. Мини-задачи

1. Создай матрицу с векторами-столбцами `v1 = [2, 0]` и `v2 = [1, 3]`.
   Напечатай каждый вектор через `A[:, ...]`.
2. Нормализуй первый столбец этой матрицы (не забудь `dtype=np.float64`!).
3. Посчитай скалярное произведение столбцов ДО и ПОСЛЕ нормализации первого.
4. Возьми `A = np.array([[0, 1], [1, 0]])` и `v = np.array([3, 7])`.
   Что даст `A @ v`? Сначала догадайся: что эта «машинка» делает с вектором?

---

## Ответы

1–3:

```python
import numpy as np
import numpy.linalg as la

A = np.array([[2, 1],
              [0, 3]], dtype=np.float64)   # v1 и v2 — по СТОЛБЦАМ
print(A[:, 0])                              # [2. 0.]
print(A[:, 1])                              # [1. 3.]
print(A[:, 1] @ A[:, 0])                    # 2.0  (до нормализации)
A[:, 0] = A[:, 0] / la.norm(A[:, 0])
print(A[:, 0])                              # [1. 0.]
print(A[:, 1] @ A[:, 0])                    # 1.0  (после)
```

4. `A @ v` даст `[7 3]` — эта матрица МЕНЯЕТ МЕСТАМИ координаты x и y
(отражение относительно диагонали).

---

Инструменты собраны. Теперь главное блюдо:
`urok_07_gram_schmidt_na_bumage.md`.
