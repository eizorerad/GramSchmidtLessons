# Урок 02. Векторы: стрелки из чисел

Цель урока: понять, что такое вектор, научиться их складывать, вычитать
и умножать на число. Это «алфавит», из которого потом соберётся Грам–Шмидт.

---

## 1. Что такое вектор

Вектор — это **стрелка**, записанная числами.

```
v = [2, 3]
```

читается как инструкция:

```
иди на 2 шага вправо и на 3 шага вверх
```

Нарисуем (каждая клетка — один шаг):

```
y
4 |
3 |         * ← конец стрелки (2, 3)
2 |       /
1 |    /
0 |_/________ x
   0  1  2  3
```

Стрелку обычно рисуют из точки (0, 0), но суть вектора — не «где он», а
**куда и как далеко он ведёт**. Вектор — это смещение.

Отрицательные числа — просто другая сторона:

```
[-2, 0]  — 2 шага влево
[0, -3]  — 3 шага вниз
```

---

## 2. Вектор в 3D и больше

Три числа — стрелка в пространстве:

```
v = [2, 3, 5]   — 2 по оси x, 3 по оси y, 5 по оси z (вглубь/вверх)
```

Четыре числа и больше — нарисовать уже нельзя, но правила ТЕ ЖЕ САМЫЕ.
Все формулы курса работают одинаково для 2, 3, 10, 100 чисел.
Поэтому учимся на 2D (можно рисовать), а применяем где угодно.

---

## 3. Сложение векторов

Складываем **по позициям**: первое с первым, второе со вторым.

```
[2, 3] + [1, 1] = [2+1, 3+1] = [3, 4]
```

Смысл: прошёл по первой стрелке, потом по второй. Итоговая стрелка —
из старта в финиш.

```
y
4 |            * финиш
3 |         *↗   ← вторая стрелка [1,1]
2 |       /
1 |    /   ← первая стрелка [2,3]
0 |_/___________ x
   0  1  2  3
```

## 4. Вычитание векторов

Тоже по позициям:

```
[2, 3] - [1, 1] = [2-1, 3-1] = [1, 2]
```

Смысл вычитания, который нам СИЛЬНО пригодится в Грам–Шмидте:

> `a - b` — это «что осталось от a, если убрать из него b».

Запомни эту фразу. Весь Грам–Шмидт — это «убрать из вектора лишнюю часть».

---

## 5. Умножение вектора на число

Каждое число вектора умножается на это число:

```
2 × [2, 3] = [4, 6]      — стрелка стала в 2 раза длиннее
0.5 × [2, 3] = [1, 1.5]  — в 2 раза короче
-1 × [2, 3] = [-2, -3]   — развернулась в обратную сторону
```

Направление не меняется (кроме умножения на отрицательное — тогда разворот),
меняется только длина. Это действие называют **масштабированием**.

Деление на число — это то же умножение: `v / 5` = `(1/5) × v`.

---

## 6. Всё то же самое в Python

```python
import numpy as np                  # подключаем numpy

v = np.array([2, 3])                # вектор v: стрелка "2 вправо, 3 вверх"
w = np.array([1, 1])                # вектор w

print(v + w)                        # [3 4]  — сложение по позициям
print(v - w)                        # [1 2]  — вычитание по позициям
print(2 * v)                        # [4 6]  — каждое число умножилось на 2
print(v / 2)                        # [1.  1.5] — каждое число поделилось на 2
```

Разбор:

- `np.array([2, 3])` — превращает список чисел `[2, 3]` в вектор, с которым
  можно делать математику. Обычный список Python так не умеет:
  `[2, 3] + [1, 1]` дал бы `[2, 3, 1, 1]` (склейку), а не сложение!
  Именно поэтому нужен NumPy.
- `print(v + w)` — NumPy сам сложил по позициям.

**Поиграйся:** поменяй числа в `v` и `w`, предскажи результат до запуска.

---

## 7. Комбинация: умножить и сложить

Самая важная конструкция линейной алгебры — «взять немного одного вектора
и немного другого»:

```
2 × [1, 0] + 3 × [0, 1] = [2, 0] + [0, 3] = [2, 3]
```

Смотри что произошло: из двух «кирпичей» `[1, 0]` (шаг вправо) и `[0, 1]`
(шаг вверх) мы собрали вектор `[2, 3]`. Числа 2 и 3 — «сколько раз взять
каждый кирпич».

Такие кирпичи называются **базис**, а числа — **координаты**. Подробнее — позже,
но идею запомни:

> координаты вектора — это рецепт: сколько взять каждого базисного вектора.

---

## 8. Мини-задачи

Считай на бумаге, проверяй в Python.

1. `[1, 5] + [2, -1] = ?`
2. `[3, 3, 3] - [1, 0, 2] = ?`
3. `3 × [2, -1] = ?`
4. `[4, 6] / 2 = ?`
5. Собери вектор `[5, 1]` из кирпичей `[1, 0]` и `[0, 1]` (сколько раз взять каждый?)
6. Что даст `-1 × [2, 3] + [2, 3]`? Сначала подумай о смысле, потом считай.

---

## Ответы

1. `[3, 4]`
2. `[2, 3, 1]`
3. `[6, -3]`
4. `[2, 3]`
5. `5 × [1, 0] + 1 × [0, 1]` — координаты и есть 5 и 1.
6. `[0, 0]` — стрелка туда и стрелка обратно. Ноль-вектор: никуда не идём.

Проверка в Python:

```python
import numpy as np
print(np.array([1, 5]) + np.array([2, -1]))       # задача 1
print(np.array([3, 3, 3]) - np.array([1, 0, 2]))  # задача 2
print(3 * np.array([2, -1]))                      # задача 3
print(np.array([4, 6]) / 2)                       # задача 4
```

---

Дальше — `urok_03_dlina_i_skalyarnoe.md`: научимся мерить длину стрелок
и проверять, перпендикулярны ли они.
