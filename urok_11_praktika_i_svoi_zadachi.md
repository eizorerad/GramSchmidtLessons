# Урок 11. Практика: решаем чужие задачи и создаём свои

Цель урока: закрепить всё. Здесь набор задач по нарастающей сложности
(с решениями) и — главное — **рецепт, как придумывать собственные задачи
и самому же их проверять**. Умение создать задачу — высшая форма понимания.

---

## Часть 1. Задачи на закрепление

Правило: сначала бумага, потом проверка кодом (`gsBasis` из урока 09).

### Задача 1 (разминка)

Нормализуй векторы: `[5, 12]`, `[0, 0, 4]`, `[1, 1]`.

<details><summary>Решение</summary>

```
|[5, 12]| = √(25+144) = 13  →  [5/13, 12/13]
|[0, 0, 4]| = 4             →  [0, 0, 1]
|[1, 1]| = √2               →  [1/√2, 1/√2] ≈ [0.707, 0.707]
```
</details>

### Задача 2 (тени)

Найди проекцию `v = [4, 2]` на `e = [0, 1]`, затем перпендикулярную часть.

<details><summary>Решение</summary>

```
v · e = 2;  тень = [0, 2];  перпендикуляр = [4, 0]
```
</details>

### Задача 3 (Грам–Шмидт 2D)

```
v1 = [2, 0],  v2 = [3, 4]
```

<details><summary>Решение</summary>

```
e1 = [1, 0]
v2 · e1 = 3;  тень = [3, 0];  u2 = [0, 4];  e2 = [0, 1]
```
</details>

### Задача 4 (Грам–Шмидт 3D)

```
v1 = [0, 0, 2],  v2 = [1, 0, 3],  v3 = [1, 1, 1]
```

<details><summary>Решение</summary>

```
e1 = [0, 0, 1]

v2 · e1 = 3;  u2 = [1, 0, 3] - [0, 0, 3] = [1, 0, 0];  e2 = [1, 0, 0]

v3 · e1 = 1  → минус [0, 0, 1]:  [1, 1, 0]
v3 · e2 = 1  → минус [1, 0, 0]:  [0, 1, 0]
u3 = [0, 1, 0];  e3 = [0, 1, 0]

Ответ: получились обычные оси z, x, y (именно в этом порядке!)
```
</details>

### Задача 5 (зависимость)

```
v1 = [1, 2],  v2 = [2, 4]
```

Что произойдёт и почему? Сколько независимых направлений?

<details><summary>Решение</summary>

`v2 = 2×v1` — та же прямая. После вычитания тени от v2 останется ноль-вектор,
второй столбец обнулится. Независимое направление одно, `dimensions` = 1.
</details>

### Задача 6 (со звёздочкой: отражение)

Зеркало вдоль вектора `[1, 1]` (вторая опорная ось — `[0, 1]`).
Построй матрицу отражения и отрази точку `[2, 0]`.

<details><summary>Решение</summary>

```python
bearBasis = np.array([[1, 0],
                      [1, 1]], dtype=np.float64)
T = build_reflection_matrix(bearBasis)   # функция из урока 10
print(T @ np.array([2., 0.]))            # [0. 2.]
```

Зеркало-диагональ меняет координаты местами: `[2, 0] → [0, 2]`.
</details>

---

## Часть 2. Как создавать СВОИ задачи

### Рецепт 1: «случайные числа + проверка кодом»

1. Придумай любые небольшие целые числа и собери из них векторы-столбцы.
2. Реши на бумаге.
3. Проверь через `gsBasis`.

Чтобы бумажный счёт был приятным, подбирай векторы, у которых длины —
«красивые» (квадрат длины — точный квадрат: 1, 4, 9, 16, 25...).
Примеры красивых заготовок:

```
[3, 4] (длина 5),  [6, 8] (10),  [5, 12] (13),  [1, 1] (√2),
[1, 1, 1] (√3),  [2, 2, 1] (3),  [1, 2, 2] (3),  [2, 3, 6] (7)
```

### Рецепт 2: «задача наоборот» (гарантированно красивый ответ)

Хочешь, чтобы ответ был заранее известен? Иди с конца!

1. Выбери готовые перпендикулярные векторы, например `p = [1, 1]` и `q = [1, -1]`
   (проверь: `p · q = 0`).
2. Собери из них «кривые» исходники:
   `v1 = 2×p = [2, 2]`, `v2 = 3×p + 5×q = [8, -2]`.
3. Ты ЗНАЕШЬ ответ заранее: `e1 = p/|p|`, `e2 = q/|q|` —
   потому что v1 смотрит вдоль p, а вычитание тени убьёт из v2 всю p-часть,
   оставив чистую q-часть.
4. Дай задачу себе (или другу): «сделай Грам–Шмидт для [2,2] и [8,-2]».

Это тот же принцип, по которому составляют задачи преподаватели.

### Рецепт 3: задача с ловушкой (зависимые векторы)

Возьми два вектора и сделай третий их суммой: `v3 = v1 + v2`.
Кто решает — должен обнаружить нулевой столбец. Отличная проверка
понимания раздела «вектор исчез» (урок 07, раздел 7).

### Рецепт 4: автоматический генератор

В папке `praktika/` лежит файл `04_generator_zadach.py`. Запусти его —
он придумает случайную задачу, даст тебе время решить, а потом покажет ответ.
Открой файл и почитай код: там всё те же `gsBasis` и `print`, ты уже всё поймёшь.
Меняй в нём размеры и диапазоны чисел — это ТВОЙ генератор.

---

## Часть 3. Чек-лист уверенности

Отмечай честно. Пункт не отмечен — вернись к уроку в скобках.

- [ ] Могу найти длину любого вектора на бумаге (01, 03)
- [ ] Могу нормализовать вектор и объяснить, зачем (03)
- [ ] Считаю скалярное произведение и знаю, что означает ноль (03)
- [ ] Могу найти тень вектора на направление и объяснить формулу `(v·e)e` (04)
- [ ] Понимаю, почему «вычесть тень = получить перпендикуляр» (04)
- [ ] Делаю Грам–Шмидт для 2–3 векторов на бумаге без подглядывания (07)
- [ ] Могу проверить ответ: длины = 1, попарные произведения = 0 (07, 08)
- [ ] Читаю строку `B[:, 1] = B[:, 1] - B[:, 1] @ B[:, 0] * B[:, 0]` и объясняю её (08)
- [ ] Могу написать `gsBasis` с циклом по памяти (09)
- [ ] Объясняю, что делает `E @ TE @ E.T` и почему читается справа налево (10)
- [ ] Знаю, почему для ортонормированной матрицы `E⁻¹ = E.T` (10)
- [ ] Придумал и решил минимум 3 собственные задачи (этот урок)

Все галочки стоят? Тогда открывай последний файл —
`urok_12_most_k_sobstvennym_vektoram.md`. Ты готов.
