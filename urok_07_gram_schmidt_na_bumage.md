# Урок 07. Процесс Грама–Шмидта на бумаге, шаг за шагом

Это центральный урок курса. Ты соберёшь Грам–Шмидт из кирпичей уроков 02–04.
Никакого кода — только руки и бумага. Код будет в следующем уроке.

---

## 1. Какую проблему решает Грам–Шмидт

Вспомни обычные оси координат x и y. Они хорошие, потому что:

1. перпендикулярны друг другу (угол 90°);
2. каждая длиной 1.

Такой набор осей называется **ортонормированный базис**. Разберём слово:

- **орто** — от «ортогональный» = перпендикулярный;
- **нормированный** — длина каждого вектора равна 1 (норма = длина, урок 03).

А теперь представь: тебе дали набор «кривых» векторов — под произвольными
углами, произвольной длины. Они описывают то же пространство, но работать
с ними неудобно (как мерить квартиру линейкой, где деления нанесены вкривь).

**Грам–Шмидт — это процедура, которая из кривого набора векторов делает
ровный: перпендикулярный и единичный.** Причём новые оси описывают
то же самое пространство, что и старые.

```
кривые v1, v2, v3   →  [Грам–Шмидт]  →  ровные e1, e2, e3
(любые углы, длины)                     (все ⊥ друг другу, все длины 1)
```

---

## 2. Вся идея в трёх предложениях

Обрабатываем векторы по очереди:

1. **Первый вектор** просто нормализуем — он задаёт первую ось.
2. **Каждый следующий**: вычитаем из него тени на все УЖЕ построенные оси
   (урок 04: «вычесть тень → остаётся перпендикуляр»), потом нормализуем остаток.
3. Повторяем, пока векторы не кончатся.

И всё. Правда всё. Дальше — только аккуратная арифметика.

---

## 3. Разминка в 2D (самый маленький пример)

Дано два кривых вектора:

```
v1 = [3, 4]
v2 = [2, 1]
```

### Шаг 1. Первая ось e1

Нормализуем v1 (урок 03: делим на длину):

```
|v1| = √(3² + 4²) = √25 = 5

e1 = v1 / 5 = [0.6, 0.8]
```

### Шаг 2. Чистим v2 от направления e1

Считаем тень v2 на e1 (урок 04):

```
v2 · e1 = 2×0.6 + 1×0.8 = 1.2 + 0.8 = 2

тень = 2 × [0.6, 0.8] = [1.2, 1.6]
```

Вычитаем тень — остаётся перпендикулярная часть, назовём её u2:

```
u2 = v2 - тень = [2 - 1.2, 1 - 1.6] = [0.8, -0.6]
```

Проверка перпендикулярности (урок 03: скалярное произведение = 0?):

```
u2 · e1 = 0.8×0.6 + (-0.6)×0.8 = 0.48 - 0.48 = 0  ✓
```

### Шаг 3. Нормализуем u2 → получаем e2

```
|u2| = √(0.8² + 0.6²) = √(0.64 + 0.36) = √1 = 1
```

Длина уже 1 (повезло), значит:

```
e2 = [0.8, -0.6]
```

### Итог разминки

```
Было:  v1 = [3, 4],  v2 = [2, 1]        — кривые
Стало: e1 = [0.6, 0.8],  e2 = [0.8, -0.6] — перпендикулярные, длины 1
```

Обрати внимание: e1 смотрит ТУДА ЖЕ, куда v1 (мы его только укоротили).
А e2 — это «то новое, что v2 добавляет к направлению v1».

---

## 4. Основной пример в 3D (классика из лекций)

Теперь три вектора — тот же пример, что разбирают в курсе линейной алгебры:

```
v1 = [1, 1, 1]
v2 = [2, 0, 1]
v3 = [3, 1, -1]
```

Здесь будут корни (√3 и т.п.) — не пугайся, урок 01 раздел 5 напомнит,
что это просто числа.

### Шаг 1. e1 из v1

```
|v1| = √(1² + 1² + 1²) = √3

e1 = v1 / √3 = 1/√3 × [1, 1, 1]
```

### Шаг 2. Чистим v2 от e1

Тень v2 на e1:

```
v2 · e1 = 1/√3 × (2×1 + 0×1 + 1×1) = 3/√3 = √3
```

(Хитрость: `3/√3 = √3`, потому что `√3 × √3 = 3`. Можно проверить на
калькуляторе: 3/1.732 ≈ 1.732.)

```
тень = √3 × e1 = √3 × 1/√3 × [1, 1, 1] = [1, 1, 1]
```

(√3 и 1/√3 сократились — осталась единица.)

Вычитаем:

```
u2 = v2 - тень = [2, 0, 1] - [1, 1, 1] = [1, -1, 0]
```

Проверка: `u2 · e1 = 1/√3 × (1 - 1 + 0) = 0` ✓

### Шаг 3. e2 из u2

```
|u2| = √(1² + (-1)² + 0²) = √2

e2 = 1/√2 × [1, -1, 0]
```

### Шаг 4. Чистим v3 — теперь от ДВУХ осей!

У v3 может быть тень и на e1, и на e2. Вычитаем ОБЕ:

```
u3 = v3 - (v3 · e1)e1 - (v3 · e2)e2
```

Тень на e1:

```
v3 · e1 = 1/√3 × (3 + 1 - 1) = 3/√3 = √3
тень1 = √3 × e1 = [1, 1, 1]
```

Тень на e2:

```
v3 · e2 = 1/√2 × (3×1 + 1×(-1) + (-1)×0) = 2/√2 = √2
тень2 = √2 × e2 = [1, -1, 0]
```

Вычитаем обе по очереди:

```
[3, 1, -1] - [1, 1, 1]  = [2, 0, -2]
[2, 0, -2] - [1, -1, 0] = [1, 1, -2]

u3 = [1, 1, -2]
```

### Шаг 5. e3 из u3

```
|u3| = √(1 + 1 + 4) = √6

e3 = 1/√6 × [1, 1, -2]
```

### Итог

```
e1 = 1/√3 × [1, 1, 1]    ≈ [0.577, 0.577, 0.577]
e2 = 1/√2 × [1, -1, 0]   ≈ [0.707, -0.707, 0]
e3 = 1/√6 × [1, 1, -2]   ≈ [0.408, 0.408, -0.816]
```

---

## 5. Как проверить ЛЮБОЙ ответ Грама–Шмидта

Ответ правильный, если выполнены две группы условий:

**Все длины равны 1:**

```
|e1| = 1,  |e2| = 1,  |e3| = 1
```

**Все пары перпендикулярны (скалярные произведения равны 0):**

```
e1 · e2 = 0,  e1 · e3 = 0,  e2 · e3 = 0
```

Проверь нашу пару e1 · e2 руками:

```
e1 · e2 = 1/√3 × 1/√2 × (1×1 + 1×(-1) + 1×0) = 1/√6 × 0 = 0  ✓
```

Этот способ проверки — твой лучший друг: посчитал — проверил — уверен.
Именно так ты будешь проверять и свои собственные придуманные задачи (урок 11).

---

## 6. Общие формулы (теперь они не страшные)

```
e1 = v1 / |v1|

u2 = v2 - (v2 · e1)e1
e2 = u2 / |u2|

u3 = v3 - (v3 · e1)e1 - (v3 · e2)e2
e3 = u3 / |u3|

u4 = v4 - (v4 · e1)e1 - (v4 · e2)e2 - (v4 · e3)e3
e4 = u4 / |u4|
...
```

Узор виден: **вектор номер N чистится от всех N-1 предыдущих осей, потом
нормализуется.** Каждое слагаемое `(v · e)e` — это тень из урока 04.

---

## 7. Особый случай: вектор «исчез»

Что если после вычитания теней осталось `[0, 0, 0]`?

Это значит: вектор **не добавлял нового направления** — он целиком собирался
из предыдущих (например, `v3 = v1 + v2`). Такой вектор называют
**линейно зависимым** от остальных.

Пример: `v1 = [1, 0]`, `v2 = [2, 0]`. Вторая стрелка смотрит туда же!

```
e1 = [1, 0]
тень v2 на e1 = (2)×[1, 0] = [2, 0]
u2 = [2, 0] - [2, 0] = [0, 0]   ← ничего не осталось
```

Нулевой вектор нормализовать нельзя (деление на ноль!). В коде этот случай
обрабатывается через `if`: «осталась заметная длина — нормализуй, иначе — запиши ноль».
Заодно так можно СЧИТАТЬ настоящие направления: сколько ненулевых столбцов
получилось — таково число независимых направлений (это называется **ранг**).

---

## 8. Задача для самостоятельного решения (обязательно!)

Сделай Грам–Шмидт руками для:

```
v1 = [1, 0]
v2 = [1, 1]
```

Подсказки-этапы: найди e1 (тут длина уже 1) → тень v2 на e1 → u2 → e2.

<details>
<summary>Решение</summary>

```
e1 = [1, 0]                        (длина уже 1)
v2 · e1 = 1×1 + 1×0 = 1
тень = 1 × [1, 0] = [1, 0]
u2 = [1, 1] - [1, 0] = [0, 1]
|u2| = 1
e2 = [0, 1]
```

Получились обычные оси x и y! Логично: v1 и был осью x, а из v2 мы убрали
всё горизонтальное — осталось чисто вертикальное.
</details>

И ещё одна, посложнее:

```
v1 = [0, 3]
v2 = [4, 4]
```

<details>
<summary>Решение</summary>

```
|v1| = 3,  e1 = [0, 1]
v2 · e1 = 4
тень = [0, 4]
u2 = [4, 4] - [0, 4] = [4, 0]
|u2| = 4,  e2 = [1, 0]
```
</details>

---

Ты только что сделал Грам–Шмидт руками. В следующем уроке
(`urok_08_gram_schmidt_v_kode.md`) те же самые шаги делает компьютер,
и ты будешь узнавать каждую строку.
