# Урок 04. Проекция: «тень» одного вектора на другой

Цель урока: понять проекцию. Это ПОСЛЕДНИЙ кирпич. После него Грам–Шмидт
соберётся за один урок, потому что весь Грам–Шмидт — это
«вычесть тень и поделить на длину», повторённое несколько раз.

---

## 1. Идея: тень от солнца

Представь: солнце светит строго сверху вниз. Ты держишь палку наклонно.
На земле появляется тень.

```
        палка v
       /
      /
     /
    /______________  земля (направление e)
    └── тень ──┘
```

- Палка — это наш вектор `v`.
- Земля — направление другого вектора `e`.
- Тень — это **проекция** `v` на `e`.

Тень показывает: **какая часть палки «лежит вдоль» земли**.

- Палка лежит на земле → тень равна всей палке.
- Палка стоит вертикально (перпендикулярно) → тени нет (точка, ноль).
- Палка наклонена → тень «средняя».

---

## 2. Формула проекции

Пусть `e` — **единичный** вектор (длина 1). Тогда проекция `v` на `e`:

```
тень = (v · e) × e
```

Читаем по частям:

1. `v · e` — скалярное произведение. Это **число**: «сколько единиц вектора v
   приходится на направление e». Длина тени.
2. `× e` — умножаем это число на вектор `e`. Число превращается обратно
   в **стрелку**: тень нужной длины, лежащая вдоль `e`.

То есть: скалярное произведение измеряет длину тени, а умножение на `e`
кладёт эту тень вдоль правильного направления.

> Почему `e` должен быть единичным? Потому что `v · e` даёт длину тени
> В ЕДИНИЦАХ ДЛИНЫ `e`. Если `e` длиной 1 — всё честно. Если `e` длиннее,
> формула завышает результат. Поэтому в Грам–Шмидте мы всегда сначала
> нормализуем, потом проецируем.

---

## 3. Пример руками

```
v = [2, 3]
e = [1, 0]     ← единичная ось x
```

Шаг 1. Длина тени:

```
v · e = 2×1 + 3×0 = 2
```

Шаг 2. Сама тень:

```
(v · e) × e = 2 × [1, 0] = [2, 0]
```

Смысл: у стрелки «2 вправо, 3 вверх» горизонтальная часть — «2 вправо».
Тень на ось x съела всё горизонтальное и проигнорировала вертикальное. Логично!

```
y
3 |        * v = [2,3]
2 |      /
1 |    /
0 |__/====______ x
     тень [2,0]
```

---

## 4. Главный трюк: вычитаем тень — остаётся перпендикуляр

А теперь фокус, на котором построен ВЕСЬ Грам–Шмидт.

Что будет, если из вектора вычесть его собственную тень?

```
v - тень = [2, 3] - [2, 0] = [0, 3]
```

Получился `[0, 3]` — строго вертикальный вектор. Он **перпендикулярен** оси x!

Проверим прибором из урока 03:

```
[0, 3] · [1, 0] = 0×1 + 3×0 = 0   ✓ перпендикулярно
```

### Почему так получилось

Любой вектор можно разложить на две части:

```
v = (часть вдоль e) + (часть поперёк e)
      = тень        + перпендикуляр
```

Вычитаем тень — остаётся чистый перпендикуляр:

```
перпендикуляр = v - (v · e) × e
```

**Выучи эту формулу.** Она — сердце Грама–Шмидта. Словами:

> «Убери из вектора всё, что смотрит вдоль e, — останется только то,
> что смотрит поперёк e».

---

## 5. Пример посложнее (не по осям)

Тени падают не только на оси координат. Пусть:

```
v = [3, 1]
e = [0.6, 0.8]      ← единичный вектор (проверь: √(0.36+0.64) = 1)
```

Шаг 1. Длина тени:

```
v · e = 3×0.6 + 1×0.8 = 1.8 + 0.8 = 2.6
```

Шаг 2. Тень:

```
2.6 × [0.6, 0.8] = [1.56, 2.08]
```

Шаг 3. Перпендикулярная часть:

```
v - тень = [3 - 1.56, 1 - 2.08] = [1.44, -1.08]
```

Шаг 4. Проверка перпендикулярности:

```
[1.44, -1.08] · [0.6, 0.8] = 0.864 - 0.864 = 0   ✓
```

Работает для любого направления.

---

## 6. То же самое в Python

```python
import numpy as np
import numpy.linalg as la

v = np.array([3, 1], dtype=np.float64)   # вектор-«палка»
e = np.array([0.6, 0.8])                 # единичное направление-«земля»

dlina_teni = v @ e                       # шаг 1: число 2.6
ten = dlina_teni * e                     # шаг 2: вектор [1.56, 2.08]
perpendikulyar = v - ten                 # шаг 3: [1.44, -1.08]

print("длина тени:", dlina_teni)
print("тень:", ten)
print("перпендикуляр:", perpendikulyar)
print("проверка (должен быть 0):", perpendikulyar @ e)
```

Разбор построчно:

- `v @ e` — скалярное произведение (значок `@` из урока 03). Результат — число.
- `dlina_teni * e` — число умножить на вектор: каждое число вектора `e`
  умножилось на 2.6. Здесь `*` правильный: мы масштабируем вектор числом.
- `v - ten` — вычитание векторов по позициям (урок 02).
- Последний `print` покажет `0.0` или что-то вроде `2e-16` — «компьютерный ноль»
  (урок 03, раздел 6).

Часто три строки схлопывают в одну:

```python
perpendikulyar = v - (v @ e) * e
```

Читай её справа налево: скалярное произведение → тень → вычли из v.
Эта строка почти без изменений появится в коде Грама–Шмидта.

---

## 7. Мини-задачи

Всюду `e` — единичный вектор.

1. `v = [5, 2]`, `e = [1, 0]`. Найди тень и перпендикулярную часть.
2. `v = [5, 2]`, `e = [0, 1]`. Найди тень. (Тень на ось y!)
3. `v = [4, 4]`, `e = [1, 0]`. Вычти тень из v и проверь перпендикулярность.
4. `v = [3, 0]`, `e = [0, 1]`. Какая тень? Почему?
5. Что получится, если спроецировать `v = [2, 6]` на направление самого себя
   (то есть на `e = v/|v|`)? Сначала подумай, потом считай.

---

## Ответы

1. Тень `= (5)×[1,0] = [5, 0]`; перпендикуляр `= [0, 2]`.
2. `v · e = 2`, тень `= [0, 2]` — вертикальная часть вектора.
3. `[4,4] - [4,0] = [0,4]`; проверка `[0,4]·[1,0] = 0` ✓
4. Тень `= [0, 0]` — ноль! Вектор `[3, 0]` перпендикулярен оси y,
   у вертикального солнца горизонтальная палка «вдоль y» тени не даёт.
5. Получится сам `v = [2, 6]`. Тень вектора на своё же направление — весь вектор.
   (Длина: `|v| = √40`; `v · e = √40`; тень `= √40 × v/√40 = v`.)

---

Всё! Кирпичи собраны: длина, нормализация, скалярное произведение, проекция.
Можно идти двумя путями:

- если хочется скорее к сути — `urok_07_gram_schmidt_na_bumage.md`;
- по порядку (рекомендую) — `urok_05_python_s_nulya.md`, чтобы код
  дальше читался легко.
