# Урок 08. Грам–Шмидт в коде: каждая строка под микроскопом

Цель урока: превратить бумажный процесс из урока 07 в программу.
Сначала — «развёрнутая» версия без циклов (каждый шаг отдельной строкой),
чтобы было видно соответствие с бумагой один в один.

---

## 1. План перевода «бумага → код»

| На бумаге (урок 07) | В коде |
|---|---|
| векторы v1, v2, v3 | столбцы матрицы: `B[:, 0]`, `B[:, 1]`, `B[:, 2]` |
| длина `\|v\|` | `la.norm(...)` |
| скалярное произведение `v · e` | `... @ ...` |
| тень `(v · e)e` | `(... @ ...) * ...` |
| «вычесть тень» | `... = ... - (... @ ...) * ...` |
| нормализовать | `... = ... / la.norm(...)` |

Ничего нового! Все детали ты уже знаешь из уроков 03–06.

---

## 2. Полный код (потом разберём каждую строку)

```python
import numpy as np
import numpy.linalg as la

verySmallNumber = 1e-14    # "очень маленькое число" для сравнения с нулём


def gsBasis3(A):
    """Грам-Шмидт для матрицы с тремя векторами-столбцами."""

    B = np.array(A, dtype=np.float64)          # (1) копия в дробных числах

    # --- первый вектор: просто нормализуем ---
    B[:, 0] = B[:, 0] / la.norm(B[:, 0])       # (2) e1 = v1 / |v1|

    # --- второй вектор: вычесть тень на e1, нормализовать ---
    B[:, 1] = B[:, 1] - B[:, 1] @ B[:, 0] * B[:, 0]    # (3) u2 = v2 - (v2·e1)e1
    if la.norm(B[:, 1]) > verySmallNumber:             # (4) осталось что-то?
        B[:, 1] = B[:, 1] / la.norm(B[:, 1])           # (5) e2 = u2 / |u2|
    else:
        B[:, 1] = np.zeros_like(B[:, 1])               # (6) зависимый → ноль

    # --- третий вектор: вычесть тени на e1 И на e2, нормализовать ---
    B[:, 2] = B[:, 2] - B[:, 2] @ B[:, 0] * B[:, 0]    # (7) минус тень на e1
    B[:, 2] = B[:, 2] - B[:, 2] @ B[:, 1] * B[:, 1]    # (8) минус тень на e2
    if la.norm(B[:, 2]) > verySmallNumber:
        B[:, 2] = B[:, 2] / la.norm(B[:, 2])           # (9) e3 = u3 / |u3|
    else:
        B[:, 2] = np.zeros_like(B[:, 2])

    return B                                           # (10) отдаём результат
```

---

## 3. Разбор построчно

### Строка (1): копия матрицы

```python
B = np.array(A, dtype=np.float64)
```

Два дела сразу (урок 06, раздел 5):

- **копия**: дальше мы перезаписываем столбцы; исходная матрица `A`
  у того, кто нас вызвал, останется нетронутой;
- **дробные числа**: без `dtype=np.float64` целочисленная матрица молча
  округлила бы все наши дроби до нуля.

### Строка (2): первая ось

```python
B[:, 0] = B[:, 0] / la.norm(B[:, 0])
```

- `B[:, 0]` — первый столбец = первый вектор (урок 06, раздел 3);
- `la.norm(B[:, 0])` — его длина;
- поделили вектор на длину — получили единичный (урок 03, раздел 2);
- результат записали ОБРАТНО в тот же столбец. С этого момента
  `B[:, 0]` — это уже `e1`, а не `v1`!

Это в точности `e1 = v1 / |v1|`.

### Строка (3): вычитаем тень

```python
B[:, 1] = B[:, 1] - B[:, 1] @ B[:, 0] * B[:, 0]
```

Читаем правую часть по кусочкам (порядок действий: сначала `@`, потом `*`, потом `-`):

1. `B[:, 1] @ B[:, 0]` — скалярное произведение v2 и e1. Число:
   длина тени (урок 04, шаг 1).
2. `... * B[:, 0]` — число умножить на вектор e1. Получилась сама тень
   `(v2·e1)e1` (урок 04, шаг 2).
3. `B[:, 1] - ...` — из v2 вычли тень. Осталась перпендикулярная часть u2.
4. `B[:, 1] = ...` — записали u2 на место второго столбца.

Одна строка кода = формула `u2 = v2 - (v2 · e1)e1` целиком.

### Строки (4)–(6): развилка «настоящий вектор или ноль?»

```python
if la.norm(B[:, 1]) > verySmallNumber:
    B[:, 1] = B[:, 1] / la.norm(B[:, 1])
else:
    B[:, 1] = np.zeros_like(B[:, 1])
```

Вспомни урок 07, раздел 7: если после вычитания теней вектор «исчез»,
значит он не добавлял нового направления, и нормализовать нечего
(делить на ноль нельзя).

Но компьютер редко выдаёт идеальный ноль — обычно микромусор вроде `1e-16`
(урок 03, раздел 6). Поэтому сравниваем не с нулём, а с порогом:

```python
verySmallNumber = 1e-14      # это 0.00000000000001
```

- длина БОЛЬШЕ порога → вектор настоящий → делим на длину (нормализуем);
- длина МЕНЬШЕ порога → считаем вектор нулевым → `np.zeros_like(B[:, 1])`
  создаёт вектор из нулей той же формы и записывает его в столбец.

### Строки (7)–(9): третий вектор чистим от ДВУХ осей

```python
B[:, 2] = B[:, 2] - B[:, 2] @ B[:, 0] * B[:, 0]   # минус тень на e1
B[:, 2] = B[:, 2] - B[:, 2] @ B[:, 1] * B[:, 1]   # минус тень на e2
```

То же вычитание тени, дважды подряд: сначала убрали составляющую вдоль e1,
потом (из того, что осталось!) — составляющую вдоль e2.
Это формула `u3 = v3 - (v3·e1)e1 - (v3·e2)e2` за два прохода.

Тонкость: во второй строке `B[:, 2]` уже не исходный v3, а «v3 минус первая
тень». Так и надо — мы чистим постепенно, результат такой же.

### Строка (10)

```python
return B
```

Функция отдаёт готовую матрицу: в её столбцах теперь лежат e1, e2, e3.

---

## 4. Запускаем и проверяем

```python
V = np.array([[1, 2, 3],
              [1, 0, 1],
              [1, 1, -1]], dtype=np.float64)   # v1, v2, v3 из урока 07 — по столбцам!

E = gsBasis3(V)
print(E)
```

Должно получиться (сравни с бумажным ответом урока 07):

```
[[ 0.577  0.707  0.408]
 [ 0.577 -0.707  0.408]
 [ 0.577  0.     -0.816]]
```

Первый столбец ≈ `1/√3 × [1,1,1]`, второй ≈ `1/√2 × [1,-1,0]`,
третий ≈ `1/√6 × [1,1,-2]`. **Бумага и код сошлись.**

### Автоматическая проверка ответа

Проверим оба условия из урока 07 (длины = 1, произведения = 0):

```python
print("длины столбцов:")
print(la.norm(E[:, 0]), la.norm(E[:, 1]), la.norm(E[:, 2]))   # все ≈ 1.0

print("перпендикулярность:")
print(E[:, 0] @ E[:, 1])   # ≈ 0
print(E[:, 0] @ E[:, 2])   # ≈ 0
print(E[:, 1] @ E[:, 2])   # ≈ 0
```

Есть и проверка одной строкой — для любопытных:

```python
print(E.T @ E)    # должна получиться почти единичная матрица
```

Почему: `E.T @ E` считает СРАЗУ ВСЕ скалярные произведения всех пар столбцов
и раскладывает их в таблицу. На диагонали — произведения столбцов с самими
собой (длины в квадрате, должны быть 1), вне диагонали — пары (должны быть 0).
Единицы на диагонали, нули вокруг = единичная матрица = всё правильно.
Это фирменный признак ортонормированной матрицы, он ещё сыграет в уроке 10.

---

## 5. Поэкспериментируй (обязательная часть!)

1. Подставь векторы из своих бумажных задач урока 07 и сверь ответы.
2. Подставь ЗАВИСИМЫЕ векторы и посмотри на нулевой столбец:

```python
V = np.array([[1, 2, 3],
              [0, 0, 2],
              [0, 0, 0]], dtype=np.float64)
# здесь v2 = 2×v1 — зависимый!
print(gsBasis3(V))
```

Второй столбец станет нулевым — функция честно сообщила: «новых направлений
тут только два».

3. Поменяй `verySmallNumber` на огромное число (например, `10`) и посмотри,
   как всё ломается. Верни обратно. Такие эксперименты дают понимание,
   ЗАЧЕМ каждая деталь.

---

## 6. Мини-задачи

1. Напиши функцию `gsBasis2(A)` для матрицы из ДВУХ векторов-столбцов
   (упрощённая копия `gsBasis3` — просто выбрось блок третьего вектора).
2. Прогони через неё разминочный пример урока 07: `v1 = [3, 4]`, `v2 = [2, 1]`.
   Сверь с бумагой (`e1 = [0.6, 0.8]`, `e2 = [0.8, -0.6]`).
3. Скажи словами (не глядя в урок), что делает строка:
   `B[:, 1] = B[:, 1] - B[:, 1] @ B[:, 0] * B[:, 0]`

---

## Ответы

1–2:

```python
def gsBasis2(A):
    B = np.array(A, dtype=np.float64)
    B[:, 0] = B[:, 0] / la.norm(B[:, 0])
    B[:, 1] = B[:, 1] - B[:, 1] @ B[:, 0] * B[:, 0]
    if la.norm(B[:, 1]) > verySmallNumber:
        B[:, 1] = B[:, 1] / la.norm(B[:, 1])
    else:
        B[:, 1] = np.zeros_like(B[:, 1])
    return B

V = np.array([[3, 2],
              [4, 1]], dtype=np.float64)
print(gsBasis2(V))
# [[ 0.6  0.8]
#  [ 0.8 -0.6]]   — совпало с бумагой!
```

3. «Из второго вектора вычитается его тень на первую ось; в столбце остаётся
   часть второго вектора, перпендикулярная первой оси».

---

Заметил, что для 4, 5, 10 векторов пришлось бы копировать блоки снова и снова?
Программисты такое не терпят. В `urok_09_obshchaya_funkciya.md` мы свернём
всё в цикл — получится универсальная функция для любого числа векторов.
