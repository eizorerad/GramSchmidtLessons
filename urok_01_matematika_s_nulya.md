# Урок 01. Математика с нуля: степени, корни, теорема Пифагора

Цель урока: закрыть три пробела, без которых дальше никак:

1. что такое «в квадрате» (степень 2);
2. что такое квадратный корень;
3. теорема Пифагора — она даст нам длину вектора.

Если ты это знаешь — быстро пробеги и реши мини-задачи в конце.

---

## 1. Степень: «в квадрате»

Запись `3²` (читается «три в квадрате») означает:

```
3² = 3 × 3 = 9
```

То есть число умножили само на себя. Ещё примеры:

```
2² = 2 × 2 = 4
5² = 5 × 5 = 25
10² = 10 × 10 = 100
```

Важный случай — отрицательные числа:

```
(-3)² = (-3) × (-3) = 9
```

Минус на минус даёт плюс, поэтому **квадрат любого числа — не отрицательный**.
Это нам очень пригодится: длина не бывает отрицательной, и квадраты — тоже.

В Python степень пишется двумя звёздочками:

```python
print(3 ** 2)    # напечатает 9
print((-3) ** 2) # напечатает 9
```

Разбор:

- `3 ** 2` — «три в степени два». Именно ДВЕ звёздочки. Одна звёздочка `*` — это обычное умножение.
- `#` — это комментарий: всё после `#` Python не читает, это заметка для человека.

---

## 2. Квадратный корень

Квадратный корень — это действие, ОБРАТНОЕ квадрату.

Вопрос корня: «какое число надо умножить само на себя, чтобы получить вот это?»

```
√9 = 3      потому что 3 × 3 = 9
√25 = 5     потому что 5 × 5 = 25
√100 = 10   потому что 10 × 10 = 100
```

Знак `√` читается «корень из».

А что такое `√2`? Такого «красивого» числа нет, это дробь:

```
√2 ≈ 1.4142...
√3 ≈ 1.7320...
```

Значок `≈` означает «примерно равно». Точного конца у этих дробей нет,
поэтому в математике часто так и пишут: `√2`, не превращая в дробь.
**Не пугайся записи типа `1/√3` — это просто число, примерно 0.577.**

В Python:

```python
import numpy as np      # подключаем numpy под именем np
print(np.sqrt(9))       # sqrt = square root = квадратный корень. Напечатает 3.0
print(np.sqrt(2))       # напечатает 1.4142135...
```

Обрати внимание: Python напечатал `3.0`, а не `3`. Точка означает,
что это дробное число (просто дробная часть равна нулю). Для нас это одно и то же.

---

## 3. Теорема Пифагора — главный инструмент этого курса

Представь прямоугольный треугольник: два катета (стороны, образующие прямой угол)
и гипотенуза (самая длинная сторона, напротив прямого угла).

```
            /|
           / |
  гипотенуза |  катет b
         /   |
        /____|
       катет a
```

Теорема Пифагора говорит:

```
(гипотенуза)² = a² + b²
```

значит:

```
гипотенуза = √(a² + b²)
```

### Пример

Катеты 3 и 4. Какова гипотенуза?

```
√(3² + 4²) = √(9 + 16) = √25 = 5
```

Ответ: 5. Запомни эту тройку чисел (3, 4, 5) — она встречается в задачах постоянно.

### Зачем это нам

Забегая вперёд: вектор `[3, 4]` — это стрелка «3 вправо и 4 вверх».
Сама стрелка — это гипотенуза! Её длина: `√(3² + 4²) = 5`.
**Длина вектора — это просто теорема Пифагора.** Вся «страшная» формула длины
вектора из следующих уроков — это она.

В Python:

```python
import numpy as np

a = 3                        # создали переменную a и положили в неё 3
b = 4                        # создали переменную b и положили в неё 4
c = np.sqrt(a**2 + b**2)     # посчитали корень из суммы квадратов
print(c)                     # напечатает 5.0
```

Разбор построчно:

- `a = 3` — знак `=` в Python это НЕ «равно», а «положи в коробку с именем a число 3».
- `a**2 + b**2` — «a в квадрате плюс b в квадрате», то есть 9 + 16 = 25.
- `np.sqrt(25)` — корень из 25, то есть 5.

---

## 4. Пифагор в трёх измерениях

А если стрелка идёт не по плоскости, а в пространстве: вправо, вверх И вглубь?
Формула просто удлиняется — добавляется третий квадрат:

```
длина = √(a² + b² + c²)
```

Пример: стрелка `[1, 2, 2]`:

```
√(1² + 2² + 2²) = √(1 + 4 + 4) = √9 = 3
```

Работает для любого числа измерений: сколько чисел — столько квадратов под корнем.

---

## 5. Дроби вида 1/√3 — не бойся их

В уроках про Грам–Шмидт будут ответы вроде:

```
e1 = 1/√3 × [1, 1, 1]
```

Это читается так: «возьми вектор [1, 1, 1] и умножь каждое его число на 1/√3».

`1/√3` — это единица, делённая на корень из трёх:

```
1/√3 = 1/1.732... ≈ 0.577
```

Значит:

```
1/√3 × [1, 1, 1] ≈ [0.577, 0.577, 0.577]
```

Математики любят запись `1/√3 × [1, 1, 1]`, потому что она точная.
Компьютер любит `[0.577, 0.577, 0.577]`, потому что он всё считает дробями.
**Это одно и то же число, просто записанное по-разному.**

---

## 6. Мини-задачи (реши на бумаге, потом проверь)

1. Чему равно `6²`?
2. Чему равно `(-5)²`?
3. Чему равно `√49`?
4. Катеты 6 и 8. Найди гипотенузу.
5. Найди длину «трёхмерной стрелки» с числами `[2, 3, 6]`.
6. Проверь задачу 5 в Python (напиши код сам, подглядывая в раздел 3).

---

## Ответы

1. `36`
2. `25` (минус на минус — плюс)
3. `7`
4. `√(36 + 64) = √100 = 10`
5. `√(4 + 9 + 36) = √49 = 7`
6. Код:

```python
import numpy as np
print(np.sqrt(2**2 + 3**2 + 6**2))   # напечатает 7.0
```

---

Всё сошлось? Отлично. Переходи к `urok_02_vektory.md` — теперь ты готов
понять, что такое вектор.
