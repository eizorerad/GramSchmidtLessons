# Урок 03. Длина вектора и скалярное произведение

Цель урока: освоить два инструмента, из которых на 90% состоит Грам–Шмидт:

1. **длина** вектора (и как сделать длину равной 1);
2. **скалярное произведение** (и как узнать, перпендикулярны ли векторы).

---

## 1. Длина вектора = теорема Пифагора

Вектор `[3, 4]` — стрелка «3 вправо, 4 вверх». Сама стрелка — гипотенуза
треугольника с катетами 3 и 4. Значит длина:

```
|v| = √(3² + 4²) = √25 = 5
```

Запись `|v|` (вектор между вертикальными палочками) читается «длина вектора v».

Для любого числа измерений — просто больше квадратов под корнем:

```
|[1, 2, 2]| = √(1 + 4 + 4) = √9 = 3
```

В Python длину считает функция `norm` («норма» — математическое название длины):

```python
import numpy as np
import numpy.linalg as la     # linalg = linear algebra, здесь живёт norm

v = np.array([3, 4])
print(la.norm(v))             # напечатает 5.0
```

Разбор:

- `import numpy.linalg as la` — подключаем «отдел линейной алгебры» библиотеки
  numpy под коротким именем `la`.
- `la.norm(v)` — «посчитай длину вектора v». Внутри — тот самый Пифагор.

---

## 2. Нормализация: делаем длину равной 1

**Нормализовать** вектор — значит сохранить направление, но сделать длину 1.

Рецепт: **раздели вектор на его длину**.

```
v = [3, 4],  длина 5
e = v / 5 = [3/5, 4/5] = [0.6, 0.8]
```

Проверим, что длина `e` равна 1:

```
|e| = √(0.6² + 0.8²) = √(0.36 + 0.64) = √1 = 1  ✓
```

Почему это работает: длина — это «масштаб» стрелки. Делишь стрелку на её
масштаб — получаешь стрелку масштаба 1.

Векторы длиной 1 называются **единичными**. Обычно их обозначают буквой `e`.
Они как «эталонные оси»: удобны тем, что не растягивают ничего лишнего.

```python
import numpy as np
import numpy.linalg as la

v = np.array([3, 4], dtype=np.float64)  # float64 = храни как дробные числа
e = v / la.norm(v)                      # делим вектор на его длину
print(e)                                # [0.6 0.8]
print(la.norm(e))                       # 1.0 — проверка сработала
```

Про `dtype=np.float64`: мы говорим NumPy «эти числа — дробные». Если оставить
целые, то при делении всё равно получатся дроби, но при работе со столбцами
матриц (позже) целочисленная матрица молча ОКРУГЛИТ наши дроби и всё сломает.
Привыкай писать `dtype=np.float64` сразу.

---

## 3. Скалярное произведение: главная операция курса

Берём два вектора, перемножаем **по позициям** и всё **складываем**.
Результат — ОДНО ЧИСЛО (не вектор!).

```
[1, 2] · [3, 4] = 1×3 + 2×4 = 3 + 8 = 11
```

Точка `·` — знак скалярного произведения (по-английски dot product).

Ещё пример, в 3D:

```
[1, 2, 3] · [4, 5, 6] = 1×4 + 2×5 + 3×6 = 4 + 10 + 18 = 32
```

В Python это делает значок `@`:

```python
import numpy as np

u = np.array([1, 2, 3])
v = np.array([4, 5, 6])
print(u @ v)              # 32 — одно число
```

### ВАЖНО: `@` и `*` — это разные вещи!

```python
print(u @ v)   # 32          — скалярное произведение: ЧИСЛО
print(u * v)   # [ 4 10 18]  — поэлементное умножение: ВЕКТОР
```

- `@` перемножил по позициям И СЛОЖИЛ → число.
- `*` перемножил по позициям и оставил как есть → вектор.

Обе операции нам понадобятся, поэтому не путай.

---

## 4. Что скалярное произведение ГОВОРИТ о векторах

Скалярное произведение отвечает на вопрос:
**«насколько два вектора смотрят в одну сторону?»**

| Результат | Смысл |
|---|---|
| большой плюс | смотрят примерно в одну сторону |
| **ноль** | **перпендикулярны (угол 90°)** |
| минус | смотрят скорее в противоположные стороны |

Проверь на осях координат:

```
[1, 0] · [0, 1] = 1×0 + 0×1 = 0     — ось x ⊥ ось y. Перпендикулярны!
[1, 0] · [5, 0] = 5                  — обе вправо, дружат
[1, 0] · [-5, 0] = -5                — одна вправо, другая влево
```

Значок `⊥` означает «перпендикулярно».

### Правило, которое надо выучить наизусть

> **Скалярное произведение равно нулю ⇔ векторы перпендикулярны.**

Это наш «прибор для проверки прямых углов». Грам–Шмидт строит перпендикулярные
векторы, а проверять их мы будем именно так: посчитал `·`, получил 0 — порядок.

---

## 5. Связь длины и скалярного произведения

Красивый факт: скалярное произведение вектора с самим собой — это его длина в квадрате.

```
v = [3, 4]
v · v = 3×3 + 4×4 = 9 + 16 = 25 = |v|²
```

То есть `|v| = √(v · v)`. Функция `la.norm` внутри делает ровно это.

---

## 6. Компьютер и «почти ноль»

Запусти:

```python
import numpy as np
import numpy.linalg as la

v = np.array([1, 1], dtype=np.float64)
e = v / la.norm(v)             # нормализовали: [0.7071..., 0.7071...]
w = np.array([1, -1], dtype=np.float64)
print(e @ w)                   # математически должен быть 0
```

Может напечататься что-то вроде `0.0` или `2.2e-16`.

Запись `2.2e-16` — это научная запись числа: `2.2 × 10⁻¹⁶`,
то есть `0.00000000000000022`. **Это ноль с точки зрения компьютера.**

Компьютер хранит дроби приближённо (как мы округляем 1/3 до 0.3333),
поэтому вместо идеального нуля бывает микроскопический «мусор».
Увидишь `e-15`, `e-16`, `e-17` — читай как «ноль». Это будет важно в коде
Грам–Шмидта, там мы даже заведём специальное «очень маленькое число» для сравнения.

---

## 7. Мини-задачи

1. Найди длину `[6, 8]`.
2. Нормализуй `[6, 8]` (напиши сам ответ-вектор).
3. `[2, 1] · [3, 4] = ?`
4. Перпендикулярны ли `[2, 1]` и `[-1, 2]`? (посчитай `·`)
5. Перпендикулярны ли `[1, 2, 3]` и `[1, 1, -1]`?
6. Чему равно `v · v` для `v = [2, 3]`? А `|v|²`?

---

## Ответы

1. `√(36+64) = √100 = 10`
2. `[0.6, 0.8]` (поделили на 10)
3. `2×3 + 1×4 = 10`
4. `2×(-1) + 1×2 = -2 + 2 = 0` — да, перпендикулярны!
5. `1 + 2 - 3 = 0` — да!
6. `4 + 9 = 13`, и `|v|² = (√13)² = 13`. Совпало, как и обещано.

Проверка в Python:

```python
import numpy as np
import numpy.linalg as la

print(la.norm(np.array([6, 8])))                       # 10.0
print(np.array([6, 8]) / la.norm(np.array([6, 8])))    # [0.6 0.8]
print(np.array([2, 1]) @ np.array([3, 4]))             # 10
print(np.array([2, 1]) @ np.array([-1, 2]))            # 0
print(np.array([1, 2, 3]) @ np.array([1, 1, -1]))      # 0
```

---

Дальше — `urok_04_proekciya_ten.md`. Это последний «кирпич» перед самим
Грам–Шмидтом: научимся находить «тень» одного вектора на другом.
